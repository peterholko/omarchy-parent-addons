# Runs the browsing history collection once a minute while any account has
# `omarchy-parent browsing` on.
[Unit]
Description=Omarchy kids mode browsing history collection, every minute

[Timer]
OnBootSec=1min
OnUnitActiveSec=1min
AccuracySec=10s

[Install]
WantedBy=timers.target
